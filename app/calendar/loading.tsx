// Loading skeleton is handled by CalendarClient's SWR loading state
// Removed standalone loading.tsx to prevent duplicate skeleton rendering
export default function CalendarLoading() {
  return null
}
